# J.A.R.V.I.S. core — Python dependencies
anthropic>=0.69            # Claude brain (tool use, streaming, prompt caching)
elevenlabs>=1.0            # TTS (British voice)
faster-whisper>=1.0        # local STT (offline, no extra API key)
sounddevice>=0.4           # mic capture + audio playback
numpy>=1.24                # audio buffer math / RMS levels
websockets>=12.0           # local WS hub to drive the HUD
python-dotenv>=1.0         # load .env secrets
tomli>=2.0; python_version < "3.11"   # config.toml parsing (3.11+ has tomllib)

# Google APIs (Gmail / Calendar / Tasks — read-only)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1
